apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: kafka-app
  name: kafka-data
spec:
  storageClassName: standard-rwo
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
